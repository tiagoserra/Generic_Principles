@{
    if (ViewBag.Errors != null)
    {
        foreach (var error in @ViewBag.Errors)
        {
            <script>
                    SendNotification('glyphicon glyphicon-warning-sign', ' ','@error', 'danger');
            </script>
        }
    }
}